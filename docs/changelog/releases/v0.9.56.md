# 发布：v0.9.56

- 发布（release-0.9.56）：同步前端 `package.json`、Tauri `tauri.conf.json` 与 Rust `Cargo.toml` / `Cargo.lock` 版本号到 `0.9.56`，纳入本轮聊天流式切换恢复修复。

- 修复（chat-stream-runtime-resume）：后端新增会话运行态快照与流式缓存累计，前端切回会话、快照接管和流绑定时主动读取后端运行态；若会话正在处理则恢复忙碌状态和助理草稿，按缓存重建等待三点、正文流式、推理与工具状态，避免切走再切回后草稿和暂停状态丢失。
