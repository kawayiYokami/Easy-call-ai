# 发布：未发布

- 修复（task-dialog）：聊天任务弹窗编辑分钟级循环任务时，不再把后端规范化后的 cron 误回显成“1 个月”。
- 优化（task-scheduler）：任务调度改为按最近到期时间等待并在任务时间轴变更时主动唤醒，同一会话的定时任务不再被旧的串行门禁拦截，触发异步委托更及时。
- 修复（delegate-task-loop）：委托线程不再暴露或允许调用 `task` 工具，避免递归创建任务导致死循环。
