# 发布：v0.11.10

- 修复（sidebar）：VS Code 侧边栏不再因为当前工作目录不匹配而隐藏本地会话，避免已有会话列表被误过滤。
- 修复（sidebar）：VS Code 侧边栏新建会话时默认继承当前 VS Code 工作目录，作为会话主工作目录传入。
- 优化（sidebar）：侧边栏主题样式改为入口静态加载，减少宿主判断分支并保持 VS Code/Web 两端行为一致。
