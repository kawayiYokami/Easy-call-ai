# 发布：v0.10.19

- 修复（chat）：侧栏拖拽宽度规则收敛；左侧会话列表宽度限制为 `160~360`，左右侧栏拖拽进入窗口边缘 `10%` 区域时显示收起提示并可直接收起。
- 优化（chat）：会话列表最后发言预览不再重复显示人格名；当侧栏宽度小于 `360px` 时自动隐藏会话时间，优先给标题留空间。
- 功能（chat）：代码块头部在复制按钮左侧新增放大按钮；点击后打开占窗体约 `95%` 的全局预览弹层，并在弹层内保持与正文一致的代码高亮渲染。
