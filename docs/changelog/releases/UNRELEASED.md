# 未发布

- 新增（chat）：Markdown 渲染器支持注脚语法，正文引用可跳转到当前消息内的注脚列表，并补齐注脚编号展示。
- 新增（chat）：Markdown 渲染器支持 README 常见的图片链接 badge 语法，并让文件阅读器按文档目录解析相对图片路径。
- 优化（chat）：Markdown 引用块改为整块 `base-300` 容器样式，并合并连续引用行，减少多行引用被分割显示的问题。
- 优化（chat）：Markdown 引用块、代码块和表格圆角跟随当前 DaisyUI 主题设置。
- 优化（task-sidebar）：统一任务相关组件命名，配置页任务卡改为 `TaskDebugCard`、聊天侧任务卡改为 `TaskCreateCard`，并让工具评估右侧栏的“本会话任务”复用聊天侧同款任务编辑卡与公共任务列表项，避免配置页调试卡与聊天任务卡混用。
- 修复（tool-review）：工具评估侧栏补丁预览改为只显示实际改动内容，补充评估意见展示，并在 `apply_patch` 成功时持久化真实命中行范围供预览复用，避免旧预览必须点详情且行号不准的问题。
- 修复（chat）：聊天窗口顶部错误条在超长报错时改为内部滚动，并固定关闭按钮位置，避免按钮被长文本挤出可点击区域。
- 优化（chat）：将接口返回的 HTML 网页型错误压缩为简短摘要，避免把整页内容直接显示到错误提示中。
