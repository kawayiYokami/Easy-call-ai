# 未发布

## 功能

- 外观新增「自动」主题模式：跟随系统深浅自动切换主题，浅色/深色可分别指定主题（含自定义主题），重启后保持；全新安装默认开启自动模式

## 重构

- 供应商页模型卡改为草稿态编辑：切换思考等级、调整上下文等参数时卡片不再闪跳或拆成多张，保存时才统一拆分
- 简单设置页移除主题切换按钮，主题统一在设置页外观中管理

## 修复

- 供应商页被禁用的思考等级在元数据加载后自动取消勾选，不再出现「勾着但禁用」的状态

## 依赖
