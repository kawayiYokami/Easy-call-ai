# 变更日志

> 此文件由 `pnpm changelog:build` 自动生成，展示最近版本的完整说明。

## 发布：v0.73.1

## 修复

- 修复 Web 端（VS Code 侧边栏）无法创建会话的问题：新建会话与草稿打开命令已接入 Web 传输层
- 修复 Web 端迁移完成确认报错、会话创建失败提示显示原始键名的问题

## 发布：v0.73.0

## 功能

- 会话存储全面升级：消息改为多行分组 + zstd 压缩存储，占用更小、读写更快；旧数据在启动后自动后台迁移，实时显示进度与完成汇总，迁移期间界面不再长时间无反馈
- 新建会话改为草稿模式：先选好部门和人格再开口，第一句话发出后草稿自动转正，并随时准备好下一个草稿；对话历史区顶部可以直接修改草稿的部门、人格和模型
- 草稿顶部新增「和谁对话」悬浮卡片：候选按人格聚合展示，选中后回车即可发送转正
- 新建草稿自动带上工作区：文件夹分节新建携带该文件夹路径，标题栏新建携带当前会话工作区，草稿已打开时同样生效
- 追问侧新建改为选择页：可选「在原会话追问」或「在空白会话追问」，支持无上文的空追问
- 「监督任务」更名为「目标」，统一命名并接入追问侧，目标对话框更精简
- 人格头像保存分辨率提升到 256×256，头像更清晰

## 修复

- 修复新建会话偶发失败、创建后新会话从列表消失的问题

## 性能

- 新建会话几乎瞬间完成，不再有可感知的等待
- 日志窗口滚动更流畅，日志量再大也不卡顿

## 发布：v0.72.0

## 功能

- 模型选择下拉改为两层：上层选模型、底部滑动条直接调节思考等级；每个模型会记住上次使用的思考等级，切换时自动恢复；模型按钮上的思考等级始终可见，模型名过长也不会被截断
- 供应商页新增模型时，按模型能力自动勾选全部可用思考等级，无需再手动逐档勾选
- 供应商页模型卡默认折叠，仅新增的模型卡默认展开，进入页面更清爽

## 修复

- 修复追问会话切换模型后不生效、刷新被还原的问题（追问会话切换模型现在会持久化到对应会话）

## 发布：v0.71.0

## 功能

- 会话维护面板新增词元账单：展示上下文构成饼图、各分类词元数与占比、当前模型最大上下文、压缩预留区与剩余可用空间。
- 提交历史行尾新增更多操作按钮：点击查看提交详情，支持复制哈希/消息、基于该提交创建分支、撤销最近提交。
- 提交历史新增 VS Code 风格时间线图：分支分叉与合并一目了然，分支标签颜色与时间线一致。
- 存储区操作收拢为行尾菜单：应用、应用并删除、删除；创建区新增「存储更改」与「存储暂存」两个明确动作。

## 修复

- 修复会话维护面板词元账单使用估算而非真实用量、以及图表悬浮提示被容器裁剪的问题。
- 修复工具调用时行号参数误写导致搜索返回错误结果的问题。
- 修复 Git 面板窗口过窄时折叠条操作按钮被裁剪的问题，按钮改为自动换行显示。
- 修复存储应用/弹出发生冲突时 Git 面板不刷新、冲突状态不可见的问题。
- 修复会话列表简化项缺失忙碌/失败/未读状态显示的问题，与完整项对齐。
- 修复会话列表来源徽章获得焦点时按回车/空格会同时切换会话的问题，现只定位到来源会话。

## 发布：v0.70.0

## 功能

- 欢迎页的足迹墙分成三张卡：成就卡展示累计 Token、单日峰值、当前连续日和最高连续日，今日卡保留 24 小时用量图表，历史卡保留年历热力图；热力色阶改为固定锚点的平滑渐变，不同年份、不同用户之间颜色深浅可直接对比。
- 设置页各选项卡卡片标题行的按钮组统一靠右对齐（如模型卡的刷新/新增按钮、关于页的检查更新/刷新等），不再因按钮数量被均分到中间。
- 刷新模型列表的结果改从界面底部状态提示条反馈：成功显示绿色、失败显示红色，自动消失，不再写进卡片内容区。
- 表格单元格里的数学公式现在能正常渲染：含转义竖线（如范数 `\|x\|`）的公式不再被误当成列分隔符而碎列，可正常以数学符号显示。
- 对话消息的 Markdown 排版新增三档布局（紧凑/适中/宽松），可在设置页或会话菜单的外观快捷菜单中切换，切换后聊天气泡的排版密度会实时变化。三档的标题字号随档位递进：档位越宽松，标题相对正文放大越多；表格仅保留行间横线，去掉了单元格竖线。
- Markdown 排版默认档位调整为适中，新装或未手动调整过布局时默认使用适中排版。
- 分段显示的消息中，段与段之间会显示一条带序号图标的分隔线，方便区分工具调用后的不同正文段落，代码块等内容不会被单独分隔。
- 会话进行中的任务指示条重新设计：顶部展示当前进展与完成进度，展开后可按状态直观看到已完成、进行中和待办的任务列表。
- 会话任务指示条改为云母质感：点击小条会平滑展开成大卡片展示任务进度，悬停可收起，移开自动收起。
- 设置页新增界面字体与代码字体选择：可从系统字体列表中挑选，界面字体应用于对话窗口文字，代码字体用于消息里的代码块，保存后所有窗口即时生效。
- DeepSeek 选择「关闭思维链」后，请求会由模型供应商适配层统一处理关闭逻辑，行为更稳定；文本供应商选项列表改为跟随模型适配层内置清单自动更新，不再手工维护。

## 修复

- 消息中的引用块不再被拆成独立分段，而是留在正文段落内随气泡一起显示；分段只针对代码块（含图表）与表格。
- 撤回消息后，其他同时打开该会话的窗口（如桌面端与 Web 端）会立即同步裁剪被撤回的内容，无需重新进入会话；撤回端本地保持原有即时刷新。
- 另一个窗口撤回的是更早的对话、而当前窗口只加载了会话后段时，现在会自动重新加载权威快照保持一致；之前这种情况下本地会静默跳过同步，导致已撤回的消息仍然残留在界面上。
- 任务指示条在折叠状态时，内部隐藏的收起按钮不再留在 Tab 焦点序列里，纯键盘用户不会把焦点移进看不见的控件；展开/收起状态现在由触发它的按钮对外声明，屏幕阅读器读取更准确。
- 无背景模式下，消息正文各分段之间保留基础段间距，段落不再贴死重叠。
- 三档 Markdown 布局不再改动代码块与行内代码的圆角，圆角统一跟随主题设置；之前切换布局会把代码块圆角改成不同数值。
- 设置页自选的界面字体与代码字体此前保存失败，切换后刷新会还原为默认；现在改动会即时持久化，重启后仍保持所选字体。
- DeepSeek 等带思考开关的模型，现在可以在思考档位里选择「不思考」来关闭思维链；之前这一档被置灰无法点选，导致无法关闭思考。
- 工具调用后的正文在流式显示时会分段换行，但消息完成或打断后却变成一整段不换行，同一内容前后渲染不一致；现在完成态与流式保持同样的分段效果。
- 关闭分段显示后，正文的左右缩进与分段模式不一致，切换开关时排版会跳动；现在两种模式的正文左右间距保持一致，仅分隔线随开关显示或隐藏。
- 会话菜单的二级菜单会错位或飞出屏幕，部分菜单项内容超出卡片边界；现在二级菜单贴合所在一级项展开、始终停留在屏幕内，宽度随内容自适应。
- 代码块中的中文等非英文字符之前会落到系统默认字体，和正文字体观感不一致；现在统一使用界面字体，代码里的英文保持等宽、非英文字符与正文界面一致。
- 设置页各选项卡的卡片样式统一：标题移入卡片顶部并加粗分隔，选项之间改为细分隔线；API 供应商、连通性测试、模型卡片、关于页、用量页、存储页等此前样式不一的卡片现在外观一致。

## 性能

- 系统字体列表改为异步加载并在应用内缓存，首次打开后再次进入秒开，不再阻塞界面。

## 发布：v0.68.0

## 功能

- macOS 与 Linux 桌面端现在可以直接使用终端命令工具（Shell 工具）：受信命令在工作区内执行，不再受文件沙箱限制，行为与 Windows 一致。

## 修复

- Web 端 git 面板恢复「撤销最近一次提交」能力。
- 配置页「允许操作电脑」「终端 shell 类型」「图片生成模型」开关改动后立即生效，无需保存或重启应用。

## 发布：v0.67.0

## 功能

- 会话菜单整理为二级分组（发起委托 / 会话分支 / 会话互动 / 消息外观），部分常用操作保留在顶层；新增「从当前会话创建分支」与「在侧边追问」两个快捷入口。
- 会话菜单新增「消息外观」子菜单，可直接切换气泡背景、消息分段、完整时间与文件换行显示，无需进入设置页。

## 修复

- 会话列表的「当前项目」分组只在 VS Code 侧边栏显示，APP 与 Web 端不再出现该分组；且分组内容只跟随 VS Code 当前打开的项目。
- 切换会话后消息行完成高度测量前显示加载覆盖层，不再闪现行重叠的布局错乱。
- 手机与窄屏窗口的输入框改为随内容自动增高，两侧按钮贴顶部对齐，与电脑端输入体验一致。

## 发布：v0.66.0

## 功能

- 会话列表的文件夹分组恢复支持拖拽调整顺序，自定义顺序会在重启后保留
- 最近会话分组默认展示至少 5 条、并含凌晨 4 点至今活跃的全部会话，支持「加载更多」每次额外显示 10 个，直到全部展示完；折叠分组后展开量重置回默认数量
- 会话列表新增「当前项目」分组，把属于当前工作区的会话集中显示，并避免在这些分组下方重复出现

## 重构

## 修复

- 配置页面操作（如保存供应商配置）现在会显示明确的成功或失败提示，不再出现点击后毫无反应的情况

## 性能

## 依赖

## 发布：v0.65.0

## 功能

- 新增 `windows` 窗口管理工具：`list windows` 列出全部可见窗口（标题/进程/位置/最小化/聚焦状态），`activate window id=xxx` 还原并切换到前台，配合截图即可定位与操作目标窗口；Windows、Linux（X11）与 macOS 可用
- 桌面脚本截图命令新增 `elements=true`：局部截图（region/focused_window）时返回区域内可交互控件列表（控件类型、名称、归一化坐标），控件带 `focused` 标记，一眼确认焦点落在哪个控件，便于精准点击；Windows、Linux（X11）与 macOS 可用
- 桌面脚本 `text` 支持中文与多行内容：中文输入改走剪贴板粘贴，不受输入法干扰、顺序稳定；引号内真实换行与 `\n` 转义均可正常写入
- 桌面脚本组合键改走真实按键事件注入，快捷键不再失效
- 电脑使用（桌面操作）新装默认关闭，需要时可在设置中开启

## 重构

## 修复

- 修复切换仓库后更改列表可能不加载的问题
- Git 面板未跟踪文件点击直接打开文件内容，不再显示空白差异
- 移除全局右键菜单屏蔽，恢复应用原生右键行为
- Git 面板文件行撤回/暂存按钮位置统一，始终靠右

## 性能

## 依赖

## 发布：v0.62.0

## 功能

- Git 面板提交行右键弹出预览卡：可复制提交哈希与提交消息，并支持基于该提交直接新建分支；最新一次提交还可一键撤销（soft，改动保留在暂存区），已推送的提交会提示无法撤销
- Git 面板不再弹出浏览器原生右键菜单，所有右键交互统一走应用内菜单
- 分支列表改为树状展示，本地与远程分支分组可折叠展开；分支删除入口移除，切换按钮移到行尾
- 更改/暂存列表的文件行图标与文件树统一
- 外观默认值调整：气泡与分段默认开启，换行显示默认关闭

## 重构

## 修复

- 修复启动界面可能停留在已经完成的初始化步骤、无法进入应用的问题
- 修复状态异常时消息发送误报失败、会话列表偶发报错的问题
- 修复 Git 面板提交预览卡在屏幕底部时超出可视区域的问题

## 性能

## 依赖

## 发布：v0.61.0

## 功能

- 个人微信渠道支持群聊消息的接收与回复，群消息不再被当作私聊处理
- 个人微信语音消息直接显示服务端转写文字，不再只是下载音频文件

## 重构

## 修复

- 修复 git 面板首次打开时数据为空、需要手动刷新才出现的问题
- 修复个人微信收不到文件：官方服务端只下发 full_url 时不再被「缺少下载参数」跳过
- 个人微信渠道联系人默认允许发送文件，无需手动开启开关
- 修复 git 更改/暂存列表的批量操作：Shift 多选后点击任一文件的暂存或撤回按钮，现在会作用于全部选中文件；列表高亮与实际选中保持一致

## 性能

- git 面板命令支持短时缓存与合并等待，桌面端与 VS Code 侧边栏同时操作同一仓库时不再重复执行相同命令
- 置顶/取消置顶会话不再卡顿，操作即时响应

## 依赖

## 发布：v0.60.1

## 功能

## 重构

## 修复

- 修复通过热键、托盘或其他入口唤出窗口时，应用可能停在“正在初始化界面”并失去响应的问题。

## 性能

## 依赖
