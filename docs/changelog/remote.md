# 变更日志

> 此文件由 `pnpm changelog:build` 自动生成，展示最近版本的完整说明。

## 发布：v0.12.3

- 优化（task）：task 工具 schema 明确要求调度时间、重复频率和结束时间只能写入 trigger，goal/why/todo 只描述单次目标与背景，避免 LLM 把调度语义混入非调度字段。
- 新增（chat）：会话工作目录显示权限等级。
- 修复（chat）：前台快照恢复增加冷却去重，避免恢复前台时重复请求同一会话。
- 修复（chat）：切换会话模型不再广播概览，单会话概览变更改为局部广播。
- 修复（chat）：前端消息读取改为工具结果占位符下发。
- 修复（chat）：截断会话预览文本长度。
- 修复（chat）：修正流式阶段状态显示时序。
- 修复（chat）：放开其他人格消息的撤回入口。
- 修复（remote-im）：修复残留渠道联系人会话复活。
- 修复（ui）：修复会话处理提示主题色。

## 发布：v0.12.2

- 新增（chat）：普通会话菜单增加新建任务入口。
- 修复（chat）：修复远程联系人自动推送通知转发。
- 修复（task）：修正任务间隔编辑回填。
- 修复（task）：到达结束时间后自动完成循环任务。
- 修复（task）：非系统会话任务回归原会话调度。

## 发布：v0.12.1

- 修复（task-dialog）：聊天任务弹窗编辑分钟级循环任务时，不再把后端规范化后的 cron 误回显成"1 个月"。
- 优化（task-scheduler）：任务调度改为按最近到期时间等待并在任务时间轴变更时主动唤醒，同一会话的定时任务不再被旧的串行门禁拦截，触发异步委托更及时。
- 修复（delegate-task-loop）：委托线程不再暴露或允许调用 `task` 工具，避免递归创建任务导致死循环。

## 发布：v0.12.0

- 修复（data-migration）：拆分通用数据迁移版本与消息仓库迁移门禁，避免普通数据读取误标旧会话消息存储已迁移。
- 修复（window）：恢复窗口布局前校验保存坐标是否仍在当前显示器范围内，避免睡眠恢复或显示器变化后窗口出现在不可见屏幕。
- 修复（chat）：会话输入面板模型切换跟随后端真实状态，避免保存失败或自动修复后显示误导。
- 修复（memory）：区分记忆重排召回门槛，避免不同召回链路混用阈值。
- 功能（memory）：记忆管理页新增召回诊断面板，支持工具召回与 RAG 双模式排查。
- 重构（data-migration）：引入数据迁移版本门禁，并预留多版本迁移 registry 骨架。
- 修复（vscode-sidebar）：更新侧边栏扩展版本号。

## 发布：v0.11.11

- 修复（chat）：窗口最小化不再冻结前台轮次状态，恢复时统一走快照路径。
- 修复（chat）：流式模式下跳过窗口恢复时的重复恢复逻辑。
- 文档（vscode-sidebar）：发布流程从 PAT 改为 Entra ID `--azure-credential`。

## 发布：v0.11.10

- 修复（sidebar）：VS Code 侧边栏不再因为当前工作目录不匹配而隐藏本地会话，避免已有会话列表被误过滤。
- 修复（sidebar）：VS Code 侧边栏新建会话时默认继承当前 VS Code 工作目录，作为会话主工作目录传入。
- 优化（sidebar）：侧边栏主题样式改为入口静态加载，减少宿主判断分支并保持 VS Code/Web 两端行为一致。

## 发布：v0.11.9

- 功能（chat）：新增会话“自动推送”，可在普通本地会话中选择远程联系人，将本会话后续所有 LLM 发言自动异步通知到目标联系人会话，并在会话控制面板显示激活提示。
- 功能（chat）：新增前台恢复投影功能，在 WebView 或浏览器标签页冻结后能够恢复流式状态，通过 `resumeProjection` 参数获取包含 `stream_cache` 的完整快照。

## 发布：v0.11.8

- 修复（meme）：后端扫描贴纸 token 生成标注，前端按标注替换渲染；远程联系人提示词移除不存在的 `contact_send_files` 工具提示，避免模型误判能力。
- 清理（chat）：精简前端流式与侧边栏刷屏日志。
- 功能（bridge）：Web 桥接认证持久化与 WebView 断连恢复、文件阅读器桥接支持。
- 功能（multimodal/tools）：新增 `read_media` 内置工具，统一承接本地图片、音频、视频解析；`read_file` 不再处理图片，改为明确提示切换到 `read_media`。
- 功能（multimodal/config）：配置页“供应商”tab 更名为“大模型”，`visionApiConfigId` 语义升级为“多模态分析模型”绑定；新增模型级 `enableVideo`，并将“音视频”开关联动为同时启用音频与视频能力。
- 功能（multimodal/routing）：前后端统一新增“协议 + 模型名”双适配路由；仅 `gemini/gemma + Gemini`、`qwen/mimo/gpt + OpenAI`、`minimax + Anthropic` 组合显示多模态开关并允许 `read_media` 执行，`auto` 按模型名推导协议族。
- 功能（multimodal/providers）：`read_media` 新增多供应商协议适配，OpenAI 家族支持音频 `input_audio` 与视频 `video_url`，Gemini 支持 `generateContent + inlineData`，MiniMax 支持 Anthropic 兼容视频输入，Mimo 视频单独按官方协议发送。
- 优化（multimodal/cache/i18n）：图片转文缓存扩成多媒体解析缓存，按“文件内容 + 多模态模型 + 解析描述”命中；相关设置、错误提示与中英繁文案统一改为“多模态分析模型 / 多媒体解析缓存 / 音视频”。
- 修复（multimodal/network）：`read_media` 的音频/视频链路改为 1 小时工具超时与 1 小时请求超时，并把底层网络错误详情直接返回给 LLM，便于解释真实失败原因。
- 功能（chat/session）：新增 `get_session` / `inform_session`，支持跨会话通知与远程联系人异步投递。
- 修复（archive）：部门缺失时跳过归档反思，避免异常中断。
- 修复（migration）：修复异常目录型会话抛弃流程永远无法成功的问题，避免迁移死循环。
- 修复（chat/store）：远程联系人恢复“10 小时未激活后下次发送前正式压缩”，并在消息仓库 block 数量或顺序异常时自动全量重写分片，避免增量写入直接失败。
- 修复（config/security）：模型刷新错误提示中的 API Key 改为脱敏显示，避免完整密钥泄漏到前端错误信息。

## 发布：v0.11.6

- 修复（chat）：撤回/重新生成补齐前端进行中保护，撤回预览改为以后端真实备份记录判断 `apply_patch` 可回滚状态，避免长会话工具历史在 UI 线程同步解析导致卡死、误判和重复触发。
- 功能（sidebar/settings）：新增独立 Web 设置页；设置侧边栏改为 DaisyUI 动态 drawer 布局并补齐 Lucide 图标，浏览器侧边栏支持直接打开设置页，设置窗口支持拖拽缩放。
- 功能（sidebar/network）：Web / VS Code 侧边栏统一复用桌面聊天布局，新增网络访问页、端口配置与远程访问密码；网络访问支持启用开关与自定义密码，关闭后 Web / IDE 桥服务停止，远程地址优先筛选真实局域网网卡并避开常见虚拟网卡。
- 优化（chat/sidebar）：新增会话控制面板，整合工作目录、远程委托状态与相关操作；聊天标题栏右侧阅读/委托入口改为常驻图标按钮，可直接切换和收起侧边栏。
- 修复（chat）：本地文件 Markdown 链接统一改由阅读侧边栏打开，支持 `path:line` 文件引用；普通 Web 侧边栏也走内置阅读栏，不再把本地链接直接交给外部环境。
- 功能（goal）：新增会话绑定 goal 续跑机制与配套系统提示词，任务到点后可启动委托并续跑到完成；补齐中断后的展示和消息形态，并收紧自动续跑触发时机，避免过度误触发。
- 修复（chat/goal）：引导消息打断流式回复时，前端按正式消息时间线合并 history flush、完成事件和后台补消息；过期轮完成不再清掉当前流式缓存，避免旧回复追加到新消息后面。
- 修复（delegate）：委托失败时支持直接查看失败原因；委托并发、控制面板交互与远程委托状态同步一并修正，减少误停、错位和状态不一致。
- 功能（agent）：支持部门多人格选择与固化执行身份；调度启动后会固化执行部门/人格来源，避免多窗口、切换会话或流式恢复时角色回复错挂到助理人格。
- 修复（chat）：本地会话不再错误注入远程 contact 工具 schema 给 LLM；远程联系人会话点击后直接切换当前会话，不再强制打开独立窗口；远程联系人会话输入区收敛为只保留新建任务按钮的精简面板。
- 重构（chat）：删除代码审查报告页面（reports tab），「发起审查」入口统一移至更多菜单；审查结果通过委托/审查面板承载，减少重复入口。
- 功能（codex）：Codex 协议供应商新增 `custom_url` 鉴权模式，自定义 URL 与 API Key 直接并入 Codex 登录卡，专门用于兼容要求 Codex CLI 请求头的第三方中转站。
- 修复（codex）：Codex 供应商改为按三种鉴权模式分别走独立分支；仅 `custom_url` 模式模拟最新 Codex CLI 请求头，官方 `read_local` / `managed_oauth` 模式恢复使用正常 PAI 请求头，不再错误伪装为 Codex CLI。
- 修复（codex）：自定义 URL 模式的运行时解析改为稳定读取 `codexCustomUrl` 与 `codexCustomApiKey`，不再错误回退到官方 URL 或空 API Key；保存链路、脏检查与设置页卡片展示同步修正。
- 修复（chat/runtime）：供应商流式请求重试策略收敛为“仅空回复重试”；429、鉴权失败和其他明确错误不再自动重试，避免错误实现导致无效补发。
- 优化（dev/changelog）：新增 `pnpm changelog:build`，更新日志改为“版本明细为源、脚本生成汇总”；引入 `docs/changelog/releases/*.md`、`docs/changelog/latest.md`、`docs/changelog/remote.md` 和 `docs/changelog/index.json`，应用内远程更新日志同步切换为读取拆分后的轻量文件，避免根 `CHANGELOG.md` 持续膨胀。
- 维护（cargo）：收紧开发测试构建缓存相关配置，减轻本地开发过程中的 Cargo 构建缓存膨胀问题。

## 发布：v0.11.5

- 重构（chat）：删除代码审查报告页面（reports tab），「发起审查」入口移至更多菜单（委托下方）；审查结果已通过委托面板展示，报告页面冗余。
- 修复（chat）：远程联系人会话隐藏完整输入面板，改为只显示新建任务按钮的精简面板，与系统通知会话行为一致。

- 修复（goal/archive）：移除应用启动时扫描 active goal 并自动续跑的行为；发送前上下文整理取消未设计的“空闲 30 分钟且用量 30%”触发条件，仅保留强制阈值保护。
- 修复（goal）：用户手动中断当前回复后暂停会话 goal 自动续跑，下一条非 goal 调度再恢复；goal 续跑消息改由系统人格发送，气泡仅显示短提示，完整续跑提示仅作为隐藏提示进入模型上下文。
- 功能（goal/delegate）：新增会话绑定 goal 机制与 `create_goal`/`update_goal` 工具，任务到点后启动委托并以 goal 续跑至完成；委托字段统一为 why/goal/todo，goal 工具常驻模型工具集，联系人专用工具仅在远程联系人会话附加。
- 功能（settings）：新增独立 Web 设置页；VS Code 侧边栏设置按钮打开桌面设置窗口，浏览器侧边栏设置按钮新开设置标签页；补齐设置页 JSON-RPC 桥接、长任务超时策略、更新进度同步和 WebSocket Origin 同源校验。
- 修复（chat）：引导消息打断流式回复时，前端按正式消息时间线合并 history flush、完成事件和后台补消息；过期轮次完成不会清掉当前流式缓存，避免出现双助理气泡同时流式、用户消息错位和旧回复追加到新消息后面。
- 修复（delegate）：`delegate` 工具模式改为 `wait/background`，明确 `wait` 可并发并等待结果；同批相同委托不再被重复调用熔断误停，停止父调度会级联中止同步委托；委托计时改由开始/完成时间前端派生，会话控制面板与右侧委托/工具审查卡片补齐清晰入口、图标和结果展示。
- 修复（chat）：调度启动后固化执行部门与人格，后端流式缓存携带 `departmentId/agentId`，前端草稿只从流式快照或正式消息继承说话人；缺失或消失的执行部门/人格会直接中断，不再兜底到助理部门，避免多窗口/切换会话时角色扮演回复显示成助理头像或名称。
- 优化（chat/sidebar）：新增会话控制面板整合工作目录与委托状态，远程/Web 端支持查看和打断委托，并将 Web 工作目录设置改为可手动输入与逐级浏览的目录选择器。
- 优化（config）：网络访问新增启用开关与自定义远程访问密码，关闭后 Web/IDE 桥服务会停止；远程地址优先筛选真实局域网网卡并避开常见虚拟网卡，远程侧边栏页面补齐 PAI 图标。
- 修复（chat）：本地文件 Markdown 链接统一由阅读侧边栏打开，支持 `path:line` 文件引用；普通 Web 侧边栏同样走内置阅读栏，仅 VS Code 扩展交给 VS Code 打开。
- 优化（chat）：聊天标题栏右侧阅读与委托入口改为常驻图标按钮，点击当前面板可收起，点击另一入口可直接切换并打开侧边栏。
- 功能（sidebar）：Web/VS Code 侧边栏改为复用桌面聊天左右栏、标题栏和输入面板，新增浏览器网络访问页、端口配置和远程访问密码；本机访问免密码，局域网/外网访问登录成功后才注册聊天广播客户端，并按访问端 viewer ID 处理会话占用。
- 修复（chat）：撤回/重新生成增加前端进行中保护，撤回弹窗的 apply_patch 可回滚判断改为后端预览真实备份记录，避免长会话工具历史在 UI 线程同步解析导致前端卡死或崩溃。
- 修复（chat）：请求体预览会话下拉复用会话显示标题解析，历史脏数据中标题等于 UUID 的会话会回退到摘要标题或时间标签，不再直接显示裸 ID。
- 修复（config）：部门配置页将 `leader` 识别为系统内置部门，主操作显示“恢复初始化”而不是“删除部门”，并支持恢复移除无关工具描述后的 leader 默认办事指南。
- 修复（chat）：调度流式过程中触发上下文压缩时，压缩 flush 事件会携带压缩前已落盘的 assistant checkpoint，并在内部重启前显式发送新一轮 `round_started`，避免界面临时只显示“压缩消息 + 新流式草稿”。
- 修复（chat）：上下文压缩/整理期间锁定当前会话输入与发送动作；若后端因竞态返回 queued，也会撤回临时草稿，避免同一条消息同时出现在队列和聊天正文。
- 修复（chat）：聊天窗口从后台恢复且后端流式已结束时，主动拉取前台轻量快照补齐最新消息，避免 WebView 后台节流漏掉完成事件后回答不显示。
- 修复（chat）：聊天窗口持久化记录最后打开的本地会话，浏览器/WebView 重新加载后优先恢复该会话；记录不存在时再回到系统通知会话，避免刷新后总是弹回系统会话。
- 优化（delegate）：调整内置 `delegate` 工具使用规则，明确有合适直属下级部门时优先委托，简单但繁琐且可独立描述的子任务适合委托；除非用户明确要求后台执行，否则默认同步等待结果，并要求主代理核验关键结论后再裁决。
- 优化（department）：重写内置 `leader` 部门办事指南，明确使用同步 `delegate` 协调下级部门，并要求核验下级关键结论后再最终回复。
- 修复（chat）：消息少于 3 条或正文少于 10K 的会话统一只显示删除入口；会话列表和“处理当前会话”弹窗不再展示归档/压缩动作，避免短会话点击后被后端拒绝。
- 修复（chat）：手动归档和归档预览只以 `conversationId` 入参解析会话，后端再从会话绑定部门派生执行人格与模型；前端点击、后端入口、拒绝原因和完成结果均补充归档日志，避免点击归档静默无反馈。
- 修复（chat）：消息仓库迁移预检不再把中断残留的 `building` manifest 当成启动阻塞异常；有旧会话 JSON 时直接重试迁移，目录内容完整时自动恢复为 ready，避免首次启动弹出误报。
- 功能（department）：新增内置预设部门 `leader`，默认使用助理人格，负责盘问需求、确定方案、拆解任务并指派下级部门；默认下级为助理部门与 `explorer`。
- 功能（task）：新建任务弹窗新增魔法棒优化按钮，可用快速模型把用户草稿结构化为完整任务草稿，自动回填标题、正文、一次性/定时模式、触发时间、重复间隔和结束时间；该主动优化请求不设置等待超时，并补充后端开始、完成、失败日志。
- 优化（model）：新增快速模型 JSON 请求 helper，传入提示词即可默认使用快速模型并返回解析后的 JSON，自动标题生成已切换复用该入口。
- 修复（task）：任务绑定系统通知会话时按系统任务处理并走独立异步委托；普通会话任务绑定会话丢失时直接标记失败，不再回退或显示为未关联会话。
- 修复（task）：一次性任务发起调度时立即完成，不再等待后续模型执行或把模型结果写回任务记录；历史已触发的一次性 active 任务会迁移为完成，未来任务不会被旧触发时间误完成。
- 修复（task）：定时任务禁止切换为一次性任务，后端更新校验和前端编辑入口同步拦截。
- 优化（chat）：系统通知会话固定显示为 `P-ai系统` 并使用系统人格头像，隐藏重命名入口，避免被最后消息或用户改名影响识别。
- 优化（chat）：任务编辑弹窗新增删除按钮和确认弹窗，删除后会同步刷新任务侧栏。
- 功能（chat）：会话左侧栏新增任务 tab，仅展示进行中的任务并按所属会话分组，点击任务可打开编辑弹窗保存标题、正文与调度。
- 功能（chat）：系统通知会话底部新增新建任务入口，支持一次性与定时任务切换，定时用一行重复间隔输入，范围覆盖每分钟到每月，任务正文前置并自动从正文生成默认标题。
- 优化（chat）：系统通知会话内发起委托时隐藏重复操作栏与审查快捷预设，但保留委托表单卡片边界，避免表单贴在输入区里不好识别。
- 优化（chat）：简化发起委托表单布局，委托内容正文前置为主输入区，背景与重点改为左右并排的辅助输入区。
- 修复（chat）：运行态指针归一到固定系统通知会话时同步确保系统通知会话分片落地，避免新配置或已分片数据只有固定 ID 指针但缺少实际系统通知会话；旧主会话仍保留为普通会话。
- 修复（chat）：将旧“主会话”语义切换为固定 ID 的“系统通知”会话；迁移时旧主会话保留为普通会话，系统通知会话新建并固定为通知接收器，禁止发言、工具栏、归档/删除/独立窗口等普通会话操作，底部仅保留发起委托与新建会话入口。
- 修复（delegate）：右侧栏委托状态改为打开时查询一次并由后端状态广播触发刷新，不再前端定时轮询或显示“正在加载委托状态”。
- 修复（config）：专家模型与部门模型解耦，部门仅默认引用 `role:expert`，不再在规范化、保存部门或聊天切换部门模型时反向覆盖专家模型设置。
- 优化（chat）：处理当前会话弹窗移除丢弃入口并精简压缩/归档说明；会话菜单的归档入口改为红色删除图标，同时归档反思只读取正文与记忆块，正文不足阈值时跳过反思。
- 修复（chat）：光速归档会立即标记会话为已归档并刷新会话列表与归档窗口，后台仅执行归档维护；空会话、无助理回复和过短会话会在归档前拦截，避免归档后被删除或列表残留。
- 修复（chat）：供应商请求返回真实用量并累加会话用量后立即推送标题栏上下文占用更新；工具续调估算用量不再广播到前端。

## 发布：v0.11.3

- 修复（i18n）：请求失败状态会展开结构化错误里的真实 `message/detail/error`，并补齐请求失败友好文案，避免界面直接显示 `status.requestFailed` 等 i18n key。
- 优化（config）：调度日志卡片化展示时间线、总用量和每轮模型调用用量；列表仅返回摘要，单轮详情中的回答、用量、工具名与末尾原始响应按页签分别懒加载，避免前端一次性承接大对象导致卡死或崩溃。
- 优化（delegate）：委托会话正文改用目录型 message_store 存储，旧 `delegate-conversations/*.json` 自动迁移为正文仓库；委托列表与详情改为摘要/块页按需读取，避免长委托会话整文件加载。
- 功能（config）：迁移页改为储存页，新增应用私有目录储存占比统计，并支持安全清理已迁移完成的旧普通会话与旧委托会话 JSON。
- 修复（appearance）：补齐界面尺寸预设和预设主题名称的 i18n 文案，避免外观页显示原始 key 或英文主题名。
- 修复（config）：供应商页将嵌入与重排拆成独立 tab，重排模型连通性测试改走重排接口，避免误走嵌入测试。
- 修复（chat）：@ 提及候选弹窗支持滚动，键盘上下切换候选时自动保持当前项可见。
- 优化（chat）：聊天窗口加载更多历史时每次补 4 条消息。

## 发布：v0.11.2

- 修复（i18n）：GOAL_TASK_WHY 清空以对齐新的 task_remind 来源判断。
- 修复（chat）：首次启动未归档会话为空时自动创建助理部门默认会话，避免发送消息时缺少 conversation_id。
- 功能（memory）：人格记忆新增自动回忆、主动记忆、关闭回忆三种模式；主动记忆关闭自动 RAG，关闭回忆会屏蔽该人格所有 recall。
- 优化（chat）：工具执行后向前端推送最新上下文用量预览，避免 UI 用量与后端工具续调压缩判断脱节。
- 修复（chat）：目标任务弹窗改为垂直结构，最近目标任务置于中间，底部固定一排实色操作按钮。
- 修复（chat）：完成态消息不再由后端把 assistant 原文解析并写入 `inlineSegments` / `memeSegments`，避免本地 Markdown 图片在流式态显示、落库替换后消失。
- 修复（chat）：部门模型角色占位仅保留在部门配置层；新建/加载/切换会话和调度读取部门模型时立即解析为具体模型 ID，避免 `role:expert` 进入会话模型、工具 session 或调度上下文。
- 优化（config）：前端切换模型和创建模型时默认上下文窗口优先设为 256K，并受已知模型最大上下文限制；最大上下文低于 200K 时不再强行拉到 256K，避免无意回到 128K。
